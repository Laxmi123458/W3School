def func():
    if True:
      print("pass") #The number of spaces is up to you as a programmer, the most common use is four, but it has to be at least one.
      print("pass") # have to use the same number of spaces in the same block of code, otherwise Python will give you an error
    else:
                      print("pass")

result=func()
print(result)